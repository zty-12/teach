# 复制本文件为 .env 并填入实际值
# 不配置也能正常使用：应用会以「纯本地模式」运行，数据只存在本机浏览器

# Supabase 项目地址（控制台 → Project Settings → API → Project URL）
VITE_SUPABASE_URL=

# Supabase anon public key（可安全暴露在前端的公开密钥）
VITE_SUPABASE_ANON_KEY=

# AI 代理端点（阶段 3 使用；建议用 Supabase Edge Function 代理，避免前端暴露 API Key）
VITE_AI_ENDPOINT=
