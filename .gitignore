node_modules
dist
dist-ssr
*.local
.vite

# 本地环境变量（含密钥，不要提交）
.env
.env.local
.env.*.local

# 日志
*.log
npm-debug.log*

# 编辑器 / 系统
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json

# OCR 训练数据（体积大，运行时按需从 CDN 获取，不必入库）
*.traineddata

# 一次性修复脚本（历史遗留）
note_v13.py
rewrite_hybrid.py

# gh-pages 部署临时目录（含 .gh-pages-deploy-v21 这类带后缀的变体）
.gh-pages-tmp/
.gh-pages-deploy*/

# 本地调试脚本与构建中转产物（不入库）
scripts/
.tmp/
tmp/

# 工作区本地数据（编辑器/助手缓存，不参与版本管理）
.workbuddy/

# Cloudflare Workers 本地构建缓存（wrangler 生成，不入库）
.wrangler/
